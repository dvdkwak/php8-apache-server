version: '3.5'

services:

  php:
    container_name: 'php-8'
    image: 'php:8-apache'
    networks:
      - web
    ports:
      - 80:80
    volumes:
      - ./root:/var/www/html
      # - ./config/apache:/etc/apache2

networks:
    web:
      driver: bridge
      name: web